#!/usr/bin/env node
import {readFile,readdir,access} from "node:fs/promises";import path from "node:path";import {fileURLToPath} from "node:url";
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),".."),manifest=JSON.parse(await readFile(path.join(root,".cursor-plugin","plugin.json"),"utf8"));if(manifest.name!=="cayos-factory"||!/^\d+\.\d+\.\d+$/.test(manifest.version))throw new Error("invalid Cursor plugin manifest");const dirs=(await readdir(path.join(root,"skills"),{withFileTypes:true})).filter(d=>d.isDirectory());for(const d of dirs){const file=path.join(root,"skills",d.name,"SKILL.md"),body=await readFile(file,"utf8");if(!body.startsWith("---\n")||!new RegExp(`^name: ${d.name}$`,"m").test(body)||!/^description: .+/m.test(body))throw new Error(`invalid skill: ${d.name}`);for(const m of body.matchAll(/\]\((references\/[^)]+)\)/g))await access(path.join(root,"skills",d.name,m[1]));}if(dirs.length!==10)throw new Error(`expected 10 skills, found ${dirs.length}`);JSON.parse(await readFile(path.join(root,"hooks","hooks.json"),"utf8"));console.log(`Validated ${dirs.length} skills and Cursor plugin manifest.`);
