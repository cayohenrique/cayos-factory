{
  "ticketProvider": { "kind": "cli", "readCommandPatterns": ["^node provider/get-task\\.mjs fake:[A-Z0-9-]+$"] },
  "browser": { "mcpServer": "chrome-agent-mcp", "debugPort": 9222 },
  "models": {
    "delivery": "inherit",
    "work": {
      "fast": "fixture-model",
      "judgment": "fixture-model"
    }
  }
}
