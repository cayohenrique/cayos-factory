{
  "name": "cayos-factory",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate-plugin.mjs",
    "test": "node --test tests/*.test.mjs",
    "audit:tokens": "node scripts/token-audit.mjs",
    "verify": "npm run validate && npm test && npm run audit:tokens"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "chrome-agent-mcp": "^1.0.0"
  }
}
