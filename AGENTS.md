# Cayos Factory contributor guidance

Keep ticket providers read-only, approval gates explicit, and verification grounded in observable behavior. Never weaken guards to make a fixture pass. Every change to scripts or hooks must add or preserve an adversarial test.
