---
name: cayos-doctor
description: Diagnose Cayos Factory configuration, provider bindings, verifier structure, Git capabilities, and proof drift.
disable-model-invocation: true
---

# Cayos Doctor

Run `node ${CURSOR_PLUGIN_ROOT}/scripts/doctor.mjs --full --root <repo>`. Explain each FAIL and stop workflow execution on BLOCKED. Do not rewrite configuration or renew proofs without explicit setup/maintenance authorization.
