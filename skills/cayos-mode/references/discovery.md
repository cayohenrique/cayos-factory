# Discovery and snapshots

Provider choice comes from project configuration. Read the ticket once with bounded comments and related artifacts required for scope. Record every external read, reason, canonical URL, revision, timestamp, and payload estimate. Revalidate at most after understanding and immediately before implementation. Material revision changes pause for renewed approval. Later agents consume snapshots, never the mutable tracker.
