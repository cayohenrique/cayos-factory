# Security and authorization

Tracker reads are allowlisted; tracker writes are forbidden. Repository writes occur only in `IMPLEMENTING`. Merge, destructive cleanup, deployment, release, publication, and automatic merge are forbidden. Push/PR are allowed only in `OPENING_PR`. Never expose secrets, full prompts, tokens, or unnecessary personal data in journals or PRs.
